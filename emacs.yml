name: emacs
root: ~/.emacs.d/

startup_window: main

windows:
  - main:
      layout: tiled
      panes:
        - git st
        - cd hugo && hugo server -D
        - cat /tmp/emacs-outdated-packages.txt
